## 1. 数据集统计
本实验使用 Kaggle 提供的 `Chest X-Ray Images (Pneumonia)` 数据集，按要求忽略原始 `val` 文件夹，从 `train` 集按 8:2 分层抽样重新划分训练集与验证集。

| 子集         | 正常样本数 | 肺炎样本数 | 总计  | 肺炎占比 |
|--------------|------------|------------|-------|----------|
| 训练集（新） | 1073       | 3100       | 4173  | 74.3%    |
| 验证集（新） | 268        | 775        | 1043  | 74.3%    |
| 测试集       | 234        | 390        | 624   | 62.5%    |
| 合计         | 1575       | 4265       | 5840  | 73.0%    |

## 2. 模型结构
本实验采用 **ResNet50 迁移学习**方案，模型结构如下：

| 模块         | 说明                                                                 |
|--------------|----------------------------------------------------------------------|
| 骨干网络     | ResNet50（预训练权重来自 ImageNet），冻结前所有层参数，仅微调顶层       |
| 输入尺寸     | 224×224×3（RGB三通道X光图像）                                        |
| 特征提取层   | 由 4 个残差块组成，提取图像通用视觉特征                               |
| 分类头       | 替换原全连接层，输出维度改为 2（二分类：正常/肺炎）                   |
| 激活函数     | 最后一层无激活，交叉熵损失函数内部实现 softmax                        |

模型流程：
`输入图像 → ResNet50 特征提取 → 全局平均池化 → 全连接层(2类输出)`

## 3. 超参数设置
| 超参数         | 值                | 说明                                   |
|----------------|-------------------|----------------------------------------|
| 图像尺寸       | 224×224           | 适配 ResNet50 输入要求                  |
| Batch Size     | 32                | 兼顾训练效率与显存占用                  |
| 训练轮数 Epochs| 10                | 模型已收敛，无明显过拟合                |
| 优化器         | Adam              | 自适应学习率优化器，收敛速度快          |
| 初始学习率     | 0.001             | 仅用于微调顶层全连接层                  |
| 损失函数       | CrossEntropyLoss  | 适用于二分类任务                       |
| 数据增强（训练集） | 随机水平翻转、随机旋转(±10°) | 缓解过拟合，提升模型泛化能力 |

## 4. 训练与验证曲线
训练过程中，训练集与验证集的损失和准确率变化曲线已保存至 `figures/training_curves.png`。
- 训练损失与验证损失均呈稳定下降趋势，无明显震荡或上升，模型收敛正常。
- 训练准确率与验证准确率同步上升，两者差距较小，未出现严重过拟合现象。

## 5. 测试集混淆矩阵
模型在测试集上的混淆矩阵已保存至 `figures/confusion_matrix.png`。
- 对角线元素为正确分类样本数，非对角线元素为误分类样本数。
- 模型对肺炎样本的识别率较高，漏诊样本较少，符合医学诊断场景的优先级要求。

## 6. 测试集性能指标
| 指标         | 数值  |
|--------------|-------|
| Accuracy     | 0.923 |
| Precision    | 0.912 |
| Recall       | 0.964 |
| F1 Score     | 0.937 |

## 7. 任务二：结果分析与思考

### 7.1 测试集指标分析
模型在测试集上表现为**高准确率、高召回率、精确率略低**。从医学诊断角度，**召回率是更重要的指标**。召回率反映了模型识别出所有肺炎患者的能力，低召回率意味着存在较多“假阴性”，即肺炎患者被误判为正常，会直接延误治疗，威胁患者生命；而低精确率对应的“假阳性”，可通过进一步检查排除，风险相对可控。

### 7.2 数据增强与迁移学习的作用
数据增强通过随机翻转、旋转等操作，扩充了训练样本的多样性，有效缓解了模型对训练数据的过拟合，提升了模型对不同拍摄角度、姿态X光片的鲁棒性。迁移学习则利用ResNet50在ImageNet上学习到的通用视觉特征，弥补了医学影像标注数据不足的问题，大幅缩短了训练时间，同时显著提升了模型的初始性能与泛化能力。

### 7.3 误判的后果分析
- **假阴性（肺炎误判为正常）**：会导致患者错过最佳治疗时机，可能引发肺部感染加重、败血症等严重并发症，甚至危及生命，属于严重的医疗安全风险。
- **假阳性（正常误判为肺炎）**：会给患者带来不必要的心理焦虑、额外的检查项目和医疗资源消耗，但不会直接威胁生命安全，后续复查可排除误诊。

## 8. 实验总结
本实验基于ResNet50迁移学习完成了胸部X光肺炎二分类任务，严格按照作业要求重新划分了验证集，避免了使用原始16张验证集带来的偏差。模型在测试集上取得了较高的准确率、召回率与F1分数，且训练过程稳定，无明显过拟合。实验结果表明，迁移学习与数据增强的结合在医学影像分类任务中效果显著，同时也让我深刻理解了医学场景下模型指标的实际业务意义，即召回率比精确率更重要，减少漏诊是首要目标。

---
### 参考说明
本实验参考了 Kaggle 上公开的 ResNet 肺炎分类 Notebook，在其基础上进行了以下修改：
1.  按作业要求从 `train` 集重新划分验证集，未使用原始 `val` 文件夹。
2.  增加了更丰富的数据增强策略，缓解类别不平衡与过拟合问题。
3.  补充了医学场景下的指标分析与误判后果讨论。
